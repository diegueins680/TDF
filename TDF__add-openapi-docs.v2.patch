diff --git a/api/openapi.yaml b/api/openapi.yaml
new file mode 100644
index 0000000000000000000000000000000000000000..4e7ac2c622aeccb2cf3293d1a716bf432eb189d1 100644
--- /dev/null
+++ b/api/openapi.yaml
@@ -0,0 +1,45 @@
+openapi: 3.0.3
+info:
+  title: TDF API
+  description: API v1 para TDF (CRM, Estudio, Label, Eventos, Escuela, Finanzas, Operación).
+  version: "1.0.0"
+servers:
+  - url: https://api.tdf.local/v1
+    description: Desarrollo local
+  - url: https://api.tdf.app/v1
+    description: Producción
+components:
+  securitySchemes:
+    bearerAuth:
+      type: http
+      scheme: bearer
+      bearerFormat: JWT
+paths:
+  /crm/contacts:
+    get:
+      summary: Listar contactos
+      responses: { "200": { description: OK } }
+  /estudio/bookings:
+    get:
+      summary: Listar bookings
+      responses: { "200": { description: OK } }
+  /label/artists:
+    get:
+      summary: Listar artistas
+      responses: { "200": { description: OK } }
+  /eventos/events:
+    get:
+      summary: Listar eventos
+      responses: { "200": { description: OK } }
+  /escuela/courses:
+    get:
+      summary: Listar cursos
+      responses: { "200": { description: OK } }
+  /finanzas/invoices:
+    get:
+      summary: Listar facturas
+      responses: { "200": { description: OK } }
+  /operacion/equipment:
+    get:
+      summary: Listar equipo
+      responses: { "200": { description: OK } }

diff --git a/docs/api.md b/docs/api.md
new file mode 100644
index 0000000000000000000000000000000000000000..14ca4d1f343d9b61aa0a8ca19f82ed3093279550 100644
--- /dev/null
+++ b/docs/api.md
@@ -0,0 +1,15 @@
+# API v1 (TDF HQ — Servant)
+Este commit añade `api/openapi.yaml` (semilla) para alinear el backend con la navegación de la UI.
+- Módulos: CRM, Estudio, Label, Eventos, Escuela, Finanzas, Operación.
+- Seguridad: `bearerAuth` (JWT).
+- Servidores: `https://api.tdf.local/v1` y `https://api.tdf.app/v1`.
+
+## Servant (sugerido)
+1. Crea módulos `TDF.API` y `TDF.Server`.
+2. En `app/Main.hs`, `serve (Proxy :: Proxy TDF.API) server`.
+3. Habilita CORS (`wai-cors`) para el origen de la UI.
+
+## Próximos pasos
+- Reemplazar respuestas dummy por esquemas reales.
+- Añadir query params: `unit_id`, `brand_id`, `site_id`, `q`, `page`, `page_size`.
+- Versionar la base como `/v1`.
